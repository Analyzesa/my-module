# my-module

A minimal Bazel module example using Bzlmod.

## Files

- `MODULE.bazel`: defines the Bazel module and dependencies
- `BUILD.bazel`: defines Bazel build and test targets

## Targets

- `//:hello`
- `//:hello_test`

## Notes

This repository was created as a minimal starter project.
